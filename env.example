# 环境变量配置示例
# 在 Cloudflare Dashboard 中设置这些变量

# 要访问的网址列表（变量名必须以 ACCESS_ 开头）
ACCESS_URL1=https://example1.com
ACCESS_URL2=https://example2.com
ACCESS_URL3=https://example3.com
ACCESS_MY_SITE=https://mysite.com
ACCESS_API_ENDPOINT=https://api.example.com/health

# 返回格式配置
RETURN_TYPE=text                    # 可选值: text, html
# RETURN_TYPE=html                  # 使用HTML格式返回美观的报告

# 通知配置
WEBHOOK_URL=https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=your-key-here
NOTIFICATION_TYPE=wechat            # 可选值: wechat, webhook, email
NOTIFICATION_ENABLED=true           # 是否启用通知，可选值: true, false

# 注意事项：
# 1. 变量名必须以 ACCESS_ 开头
# 2. 变量值必须是完整的 URL（包含 http:// 或 https://）
# 3. 不要在值周围加引号
# 4. 这些变量会在每天早上 6 点被自动访问
# 5. RETURN_TYPE 决定返回格式：text(纯文本) 或 html(美观的HTML报告)
# 6. NOTIFICATION_TYPE 决定通知方式：wechat(微信机器人), webhook(通用webhook), email(邮件)

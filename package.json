{
  "name": "cron-day-report",
  "version": "1.0.0",
  "description": "Cloudflare Worker for scheduled URL access and WeChat notification",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "wrangler dev --local",
    "tail": "wrangler tail",
    "serve": "node docker/server.mjs"
  },
  "keywords": [
    "cloudflare",
    "worker",
    "cron",
    "wechat",
    "monitoring"
  ],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "better-sqlite3": "^11.0.0"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}

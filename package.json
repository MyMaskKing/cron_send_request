{
  "name": "cron-send-request",
  "version": "1.0.0",
  "description": "Cloudflare Worker for scheduled URL access and WeChat notification",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "wrangler dev --local",
    "tail": "wrangler tail"
  },
  "keywords": [
    "cloudflare",
    "worker",
    "cron",
    "wechat",
    "monitoring"
  ],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "wrangler": "^3.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
